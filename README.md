# Max_Z——一个基于C语言的玩具型操作系统

Max_Z 是一个从零开始构建的简易操作系统内核，运行于 x86 架构。它旨在通过实践探索操作系统底层原理，包括引导启动、内存管理、中断处理和 ACPI 电源管理等核心概念。目前尚处于**预览阶段（Preview）**，支持基本的交互式命令。

> 部分基础框架代码由 DeepSeek V4 Flash 辅助生成。

## 许可证

本项目采用 **GNU Affero General Public License v3.0 (AGPL-3.0)** 授权。

版权所有 (C) 2026 ZhangMaixuan

这意味着：

- **代码完全公开**：任何人都可以查看、学习、使用和修改本项目的源代码。
- **严格的“传染性”开源（Copyleft）**：如果您修改了本项目的代码，或者基于本项目开发了衍生作品，并对外分发或提供使用，**您必须将您修改后的完整源代码，同样以 AGPL-3.0 许可证公开**。
- **网络服务（SaaS）漏洞已堵死**：即使您不直接分发二进制文件，而是将修改后的内核部署为云服务或后台服务（通过网络交互），**您也必须向该服务的所有用户公开对应的源代码**。
- **商业使用限制（本质）**：本许可证允许商业使用，但**严格禁止**将本项目的修改版本进行**闭源**处理，或将其封装为**专有商业产品**（例如类似 CEC-IDE 那种“套壳并闭源”的行为）。如果您想将 Max_Z 用作商业产品的基础，唯一的合法途径是将您的全部改动也以 AGPL-3.0 开源。

完整的授权条款，请查看项目根目录下的 [LICENSE](./LICENSE) 文件。

## 性能要求

- 需要4MB及以上内存
- CPU需要满足x86架构

## 运行截图 This is Preview.📸 

![](./image/截图%202026-08-22%2011-21-28.png)

![](./image/截图%202026-08-22%2011-21-49.png)

## # ✨ 已实现功能

### 1. 命令系统

| 命令 | 语法 | 功能描述 |
| :--- | :--- | :--- |
| **echo** | `echo [字符串]` | 在屏幕上输出指定的文本。支持带引号（`"Hello"`）或不带引号（`Hello`）两种写法。 |
| **clear / cls** | `clear` 或 `cls` | 清空当前屏幕终端的全部内容，将光标重置至左上角。 |
| **create** | `create <路径> [内容]` | 创建文件（单段名字 ≤31 字符，内容 ≤256 字节）。内容含空格时用双引号括起，如 `create notes "hello world"`；空文件用 `create notes ""`。 |
| **cat** | `cat <路径>` | 读取并显示文件内容。 |
| **mkdir** | `mkdir <路径>` | 创建目录（支持多级路径，如 `mkdir docs/sub`）。 |
| **cd** | `cd <路径>` | 切换当前目录（提示符随之变化），支持 `..`（上级）、`/`（根）、相对或绝对路径。 |
| **ls** | `ls [路径]` | 列出目录内容及大小；子目录名带 `/` 后缀，大小列显示 `-`。 |
| **addpath** | `addpath <目录>` | 把目录加入"全局路径"列表（类似 Linux PATH，上限 8 个）；`cat` 在当前目录找不到文件时按注册顺序回退查找。 |
| **listpath** | `listpath` | 列出全部已注册的全局路径。 |
| **rm / delete** | `rm <路径>` | 删除文件或空目录（`delete` 为旧名，两者等价）；非空目录、根目录、当前目录拒绝删除。 |
|**exit**|`exit`|可以实现ACPI断电，QEMU进程以退出码0关闭|

> 注：文件系统为纯内存实现（静态数组，文件与目录共用一张表），重启后数据丢失；命令前缀必须完整（`clearxxx` 不再误命中 `clear`）。

### 2. 内核基础特性
- **Multiboot 协议兼容**：可通过 GRUB 引导加载程序启动。
- **ACPI 初步支持**：包含 `acpi.c/h` 模块，为后续电源管理和硬件枚举打下基础。
- **基础终端交互**：提供简易的命令行读取与解析循环。
- **多级目录内存文件系统**：包含 `fs.c/h` 模块（"万物皆文件"简化版：目录与文件共用一张静态数组表，兄弟链表管理子项），提供 create/mkdir/read/delete/list/cd/pwd 操作接口。

## 项目结构

```bash
.
├── acpi/               # ACPI 电源管理模块
│   ├── acpi.c
│   └── acpi.h
├── fs/                 # 文件系统模块（含字符串工具）
│   ├── fs.c            # 多级目录内存文件系统实现（路径解析 + 兄弟链）
│   ├── fs.h
│   ├── str.c           # 字符串工具（strlen/strcmp/strncmp/strcpy/itoa_dec）
│   └── str.h
├── image/              # 截图资源
├── kernel/             # 内核核心（引导与链接）
│   ├── boot.asm        # 引导入口与栈设置
│   ├── linker.ld       # 链接脚本（基址 1MB）
│   └── multiboot_header.asm
├── main.c              # 内核入口、终端输出、键盘与命令解析
├── tools/              # 开发工具
│   └── fs_test.py      # 自动化冒烟测试（无头 QEMU + QMP 注入按键）
├── grub.cfg            # GRUB 启动配置（打包进 ISO）
├── Makefile            # 构建脚本（产物统一输出到 bin/）
└── README.md

（构建产物统一生成在 bin/ 目录下，含 kernel.elf 与 maxzos.iso）
```

> 模块划分：文件系统相关改动只需动 `main.c` 与 `fs/` 目录；ACPI 相关改动只需动 `main.c` 与 `acpi/` 目录。

## 编译和使用指南

### 适用于Linux

**准备工具**

请确保你的电脑里安装有
- `qemu-system-x86`（一个模拟器软件）
- `gcc`（一个C语言的编译器）
- `nasm`（汇编器）
- `binutils`（二进制工具集）

对于arch系的用户，推荐再安装一个`binutils`。

关于制作可启动镜像的工具
- `grub`引导程序
- `xorriso` ISO景象制作工具
- `mtools` FAT文件系统工具集

**编译方式**

- 构建系统并创建ISO
```bash
make
```
- **运行系统**： `make run`或者`make run-iso`
- **自动化测试**（无头 QEMU 冒烟测试，47 项用例）：`make test`
- 清理构建文件
```
make clean
```